<%= form_for(@user_sim_datum) do |f| %>
    <% if @user_sim_datum.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@user_sim_datum.errors.count, "error") %> prohibited this user_sim_datum from being saved:</h2>

          <ul>
            <% @user_sim_datum.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <%= f.hidden_field :user_id, :value => current_user.id %>
    <%= f.hidden_field :simulation_id, :value => @simulation.id %>

    <br>
    <label>Select Programme</label>
    <%#= select_tag "programme_name", options_from_collection_for_select(@simulation_data, "id", "programme_name"), prompt: "Select Programme" %>
    <%#= f.select :simulation_id, @simulation_data.map{ |c| [c.programme_name, c.id] } %><br />
    <%#= f.select :simulation_id, options_for_select(@simulation_data.map{ |c| [c.programme_name, c.id] }) %>
    <%= f.select :simulation_datum_id, options_for_select(@simulation_data.map{ |c| [c.programme_name, c.id] }) %>

    <%= f.label :no_of_slots %>:
    <%= f.text_field :no_of_slots %><br />

    <div class="actions">
      <%= f.submit "Submit" %>
    </div>
<% end %>
